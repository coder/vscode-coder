// import * as assert from "assert"
import * as vscode from "vscode"
// import * as install from "./install"

suite("Install", () => {
  vscode.window.showInformationMessage("Start install tests.")

  // TODO: Implement.
  test("install")
})
