{
	"extends": "../tsconfig.packages.json",
	"compilerOptions": {
		"resolveJsonModule": true,
		// Prevent relative imports escaping this package.
		"rootDir": "."
	},
	"include": ["src", "storybook", "storybook.preview.ts"]
}
